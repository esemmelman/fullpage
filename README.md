# Fullpage links

A compact, column-based view of the links stored in the existing Link app. Sign in with the same Supabase account. The page reads only link titles and URLs; the existing per-user Row Level Security policy protects the table.

The layout fills each column from top to bottom, then continues to the right. Long lists can be scrolled horizontally.
